# Local SQLite (current dev default)
DATABASE_URL="file:./dev.db"

# Production / Neon Postgres example — uncomment + paste your Neon URL when deploying.
# DATABASE_URL="postgresql://<user>:<password>@<host>/<db>?sslmode=require"
